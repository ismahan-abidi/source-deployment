---
- name: operate minikube
  hosts: k8s_groups
  tasks:
  - name: start minikube
    shell: "minikube start --driver=none"
    when: action=="start_minikube"
  - name: stop minikube
    shell: "minikube stop"
    when: action=="stop_minikube"